let digit  = ['0'-'9']
let letter = ['a'-'z''A'-'Z']

rule gettoken =
    digit+                     { return NUM }
  | letter (letter | digit)*   { return ID }
  | '+'                        { return PLUS }
  | [' ''\t']+                 { return WS }
